import styled from 'styled-components';

const SportCategoryStyles = styled.div`
    display: inline-grid;
    padding: 10px;
  margin-left: 5rem;
    margin-right: 20px;
    border: 1px solid var(--lightGray);
    border-radius: 10px;
p {
  margin-right: 10px;
}
`;

export default SportCategoryStyles;
