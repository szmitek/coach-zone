import styled from 'styled-components';

const Group = styled.div`
  /* Set the width and height of the group */
  width: 200px;
  height: auto;

  /* Add padding and margin to the group */
  padding: 5px;
  margin: 0 10px;

  /* Add a border and background color to the group */
  border: 1px solid #ccc;
  background-color: #eee;

  /* Style the heading */
  h2 {
    font-size: 30px;
    font-weight: bold;
    margin: 0;
    padding: 10px 0;
    position: center;
  }

  /* Style the list */
  ul {
    list-style: none;
    margin: 0;
    padding: 0;
  }
`;

export default Group;
